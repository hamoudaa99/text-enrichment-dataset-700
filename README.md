# Text Enrichment Dataset (700 examples)

This repository contains a dataset of 700 text enrichment examples.

Each example is composed of:
- a short input sentence
- an enriched, more descriptive output sentence

## Format

The dataset is stored in JSON format inside a `.txt` file:

```json
{
  "input": "...",
  "output": "..."
}
